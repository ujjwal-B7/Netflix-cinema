import { Movie } from "@/utils/types";

const Modal = ({ filteredTrendingMovie }: { filteredTrendingMovie: Movie }) => {
  return (
    <div className="h-screen w-full bg-black/50 absolute top-0">Modal</div>
  );
};

export default Modal;
